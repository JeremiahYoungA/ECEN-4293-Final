1. Install Requirements, including Visual Studio C++14
    pip install -r requirements.txt or py -m pip install -r requirements.txt
2. python setup.py build_ext --inplace
3. run any file, visualize_board.py allows playing against the MCTS AI